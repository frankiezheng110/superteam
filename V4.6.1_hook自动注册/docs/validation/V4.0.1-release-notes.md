# SuperTeam V4.0.1 Release Notes

- fixed command registration for plugin slash commands
- switched command identifiers to valid plugin skill names
- current supplement commands are `/superteam:g1`, `/superteam:g2`, `/superteam:g3`
